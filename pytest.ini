[pytest]
testpaths = tests
addopts = -p no:cacheprovider --strict-config --strict-markers -ra
markers =
    unit: deterministic test that does not require local or external services
    integration: test that requires an explicitly configured local dependency
