apiVersion: 1

providers:
  - name: Repo Search
    folder: Repo Search
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
